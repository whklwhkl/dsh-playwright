# dependencies
node_modules/

# npm local cache (used when ~/.npm is unwritable)
.npm-cache/

# runtime artifacts
shots/
*.log

# misc
.DS_Store

# pnpm local store and lockfile (library package, lockfile not tracked)
.pnpm-store/
pnpm-lock.yaml
